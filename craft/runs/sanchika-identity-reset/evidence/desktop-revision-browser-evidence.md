# Desktop revision browser evidence — 2026-07-23

The built static gallery was captured locally at **1440 × 1000** after the
in-stage proof revision. These clean viewport WebPs replace the earlier
desktop PNGs that included browser chrome and an unrelated overlay:

- `living-collection-desktop-revision.webp` (61,032 bytes)
- `witness-joint-desktop-revision.webp` (54,440 bytes)
- `craft-language-desktop-revision.webp` (50,222 bytes)

Each route renders the identity composition and the opening portion of its real
HTML contract within one viewport. The files prove the current revision renders;
they do not qualify a direction, override review findings, establish keyboard
focus, or replace human validation.
