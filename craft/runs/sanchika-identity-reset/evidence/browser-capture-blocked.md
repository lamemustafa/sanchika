# Browser capture capability interruption

The three environment-gated routes compiled as static HTML with the real
`ReviewDeskPreview` contract. Browser capture did not run: the execution
sandbox rejected local loopback binding for the Astro dev server, and the
Playwright CLI wrapper did not become available in this environment. This is a
capability interruption, not a visual or accessibility pass.

Before review can begin, capture each direction at desktop and 390px mobile,
then complete keyboard, forced-colors, and reduced-motion checks. Retain those
captures beside this record and resume this run at `explore` without changing
the current directions or acceptance thresholds.
